import styled from 'styled-components'

export const SingleMachineContainer = styled.button`
    width: 100%;
    display: grid;
    outline: none;
    border: none;
    padding: 2rem 0;
    text-align: start;
    background-color: inherit;
    &:hover {
        background-color: rgba(115, 102, 255, 0.1);
    }
    .center {
        text-align: center;
    }
`
